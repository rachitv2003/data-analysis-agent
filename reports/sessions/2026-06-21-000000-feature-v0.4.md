# Session Report — 2026-06-21
## Branch: feature/v0.4

### Context
- Starting new v0.4 branch from main
- Previous work: v0.1 released, v0.2 feature branch active
- Goal: Begin v0.4 development phase

### Status
- [x] Branch created: `feature/v0.4`
- [x] Branch pushed to origin
- [x] Spec reviewed — drift audit complete (see below)
- [ ] Work started

---

## Drift Audit — /spec-check — 2026-06-21

**Result: DIVERGENCES FOUND**

### High Severity

| Spec File | Claim | Code Reality |
|-----------|-------|-------------|
| `spec/product/05-api.md` | Sending `/ask` with no `dataset_id` or `dataset_ids` should 422 | Integration test `test_ask_no_dataset_id_returns_error` asserts `status_code == 422`, but the code routes to C19 auto-selection (400 "no_datasets" if nothing uploaded, not 422). The test assertion is wrong. |

### Medium Severity

| Spec File | Claim | Code Reality |
|-----------|-------|-------------|
| `spec/product/07-agent-graph.md` | `setup` node reads `dataset_id` (singular) from state | Code uses `dataset_ids` (plural list) in state. Spec wording is stale. |
| `spec/product/05-api.md` | `suggested_questions` in `/ask` response is always 3 strings | `generate_suggestions()` may return fewer than 3 or empty on LLM failure. No enforcement. |
| `spec/product/05-api.md` | `DELETE /datasets/{id}` blocked (409) if any run against it is "running" | Code only checks `QueryRunRow.dataset_id == dataset_id`, not `dataset_ids_json`. Multi-dataset runs bypass the guard. |

### Low Severity

| Spec File | Claim | Code Reality |
|-----------|-------|-------------|
| `spec/product/07-agent-graph.md` | Stub iteration detection for `<node:plan>` | Stub counts `"Result:" + "Error:"` occurrences in the prompt to detect iteration. Could misfire if dataset context contains those strings. |
| C17 | Server-side filename-based notes detection (`_notes.txt`, `context.txt`) | No server-side filename detection; this is client-side only per spec — minor wording ambiguity. |

### Missing Tests

- `PATCH /sessions/{id}/name` — no test
- `DELETE /sessions/{id}` and `DELETE /sessions` — no tests
- `GET /memory`, `PATCH /memory` — no tests
- `POST /datasets/{id}/clean`, `POST /datasets/{id}/clean/apply` — no tests
- `GET /runs/current` — no test
- `GET /sessions` (list all) — no test
- `force_finalize` via 3 consecutive errors — no test
- `force_finalize` via max iterations → `is_best_effort=True` — no test
- Rate-limit retry (Gemini 3× / OpenRouter linear) — no tests
- Settings singleton isolation between test runs — potential flakiness

### Undocumented Behavior

- Best-effort runs (`force_finalize`) write `status="completed"` in DB → counted in daily stats. Not documented in stats spec or agent-graph spec.
- Selector shortcut: when only 1 dataset exists, `select_datasets` returns immediately with no LLM call and `selector_reasoning=None`. Spec implies selector always runs when no IDs are supplied.
- `MAX_SESSION_TURNS = 20` is hardcoded in `runner.py` with no env-var override. Not documented in spec.

### Next Steps
1. Fix high-severity: correct test assertion in `test_pipeline.py` for no-dataset-id path
2. Update `07-agent-graph.md` setup node to say `dataset_ids` (plural)
3. Decide: enforce exactly 3 `suggested_questions` in code, or relax spec
4. Decide: extend running-guard to check `dataset_ids_json` in `DELETE /datasets/{id}`
5. Add missing tests for sessions / memory / clean / runs endpoints
